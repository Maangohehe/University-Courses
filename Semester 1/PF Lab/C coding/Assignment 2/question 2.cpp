#include <stdio.h> //First we will add the header file.

int main()
{
	//For increament by 2 we use 2++before variable.
	int a= 15;
	++a; //++a means that a will be increamented by 2.
	++a; // By adding 2 ++a 
	printf("increament of a by 2 = %d", a); //Here the output will be provided with 15 increamented by 2.
}
