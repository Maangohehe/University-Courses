#include <strings.h>
#include <stdio.h>
int main()
{
	int shopping_list= 0;
	do
	{
		printf("What should be added to the shopping list?");
		scanf("%d", &shopping_list);
		if(shopping_list= 10);
		printf("Tea is added.");
		
		}
		 
		
	while(shopping_list= 10);
}
